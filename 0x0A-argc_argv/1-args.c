#include <stdio.h>

/**
 * main - prints the number of arguments passed into it
 * @argc:
 * @argv:
 *
 * Return: 0 - success
 */

int main(int argc, char *argv[] __attribute__((unused)))
{
	printf("%d\n", argc);

	return (0);
}
